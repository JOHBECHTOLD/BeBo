# ============================================
# BeBo Umgebungsvariablen / Environment Variables
# ============================================
# DE: Kopiere diese Datei zu .env und fülle die Werte aus
# EN: Copy this file to .env and fill in the values
#
# cp .env.example .env

# Django Core
# -------------------------------------------
# DE: Generieren mit / EN: Generate with:
# python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=CHANGE-ME-to-a-long-random-string

# DE: In Production auf False setzen!
# EN: Set to False in production!
DEBUG=True

# DE: Komma-separierte Liste / EN: Comma-separated list
ALLOWED_HOSTS=localhost,127.0.0.1

# DE: Für HTTPS (komma-separiert) / EN: For HTTPS (comma-separated)
CSRF_TRUSTED_ORIGINS=http://localhost:8000

# Datenbank / Database
# -------------------------------------------
DB_NAME=bebo_db
DB_USER=bebo_user
DB_PASSWORD=CHANGE-ME-to-secure-password
DB_HOST=db
DB_PORT=5432